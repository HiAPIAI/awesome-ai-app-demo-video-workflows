# Contributing

Read `AGENTS.md` before making changes. Keep contributions inside one ownership lane and run `npm test` before opening a pull request.

Workflow contributions must use original or clearly licensed assets, keep every path project-relative, and include a full-video human review. Generated binaries, API keys, signed URLs, local absolute paths, and paid-task journals must never be committed.
