# Workflow lane: examples

The workflow worktree will add five original, rights-clean examples. Each example must include `demo.yaml`, project-relative assets, expected intent, and a human review note. Generated video files stay out of Git.
